import XCTest
@testable import InAppPurchasesViewController

final class InAppPurchasesViewControllerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(InAppPurchasesViewController().text, "Hello, World!")
    }
}
